# Copiar este archivo como .env y ajustar los valores
# Puerto en el que se levanta el servidor Express
PORT=8080

# Ruta (relativa a la raíz del proyecto) del archivo JSON de persistencia
SERVICES_FILE=src/data/services.json
